# Codex 안정화 작업일지

## 현재 상태

- 현재 브랜치: `codex-stabilization`
- 기준 커밋: `38fc56d`
- 현재 단계: 5단계 공지사항 UI 분석 예정
- 마지막 완료 커밋: `6958b8f 개선: 사용자 공지사항 목록 및 상세 스크롤 보강`
- 다음 작업: 관리자 공지 목록 세로 스크롤과 상세 본문 가독성 보강
- 사용자 테스트 대기 여부: 아니요

## 작업별 기록

### 2026-07-12 / 0단계 네비게이션 초기화

- 상태: 사용자 Eclipse 테스트 통과
- 분석 내용: `MainUserPanel` 생성자에서 `showPanel("HomeDaily")`가 네비게이션 바 생성보다 먼저 호출되어 `NavUtil.selectHomeButton()`이 초기화 전 버튼 배열을 접근했다.
- 수정 파일: `src/main/MainUserPanel.java`
- 변경 내용: 네비게이션 바 생성 및 추가를 초기 `HomeDaily` 표시보다 먼저 수행하도록 호출 순서를 보정했다.
- 검증 명령: `git diff --check`, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `0c02a4d 수정: 사용자 메인 네비게이션 초기화 순서 보정`
- 남은 위험: 이번 네비게이션 초기화 수정 범위에서 확인된 위험 없음
- 사용자 확인 필요 사항: 확인 완료. 홈·캘린더·운동·내 정보 전환이 정상이며 초기화 전 네비게이션 경고가 더 이상 출력되지 않는다.

### 2026-07-12 / 1단계 식사 유형 누락 저장 차단

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: `HomeMealPanel`의 식사 카드가 유형을 설정하지만 `FoodListPanel.saveMealToDatabase()` 자체에는 빈 식사 유형 검증이 없어 불완전한 값이 `MealDAO.insertMeal()`로 전달될 수 있었다.
- 수정 파일: `src/panel/FoodListPanel.java`
- 변경 내용: 식사 유형이 `null` 또는 공백이면 경고 안내 후 DAO 호출 전에 저장을 중단하도록 보강했다.
- 검증 명령: `git diff --check`, 식사 유형과 `insertMeal()` 호출 순서 정적 검색, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 식사 유형 검증이 DAO 호출보다 먼저 수행됨을 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `e86b2dd 수정: 식사 유형 누락 저장 차단`
- 남은 위험: 경고 대화상자의 실제 표시와 저장 미실행 여부는 체크포인트 1에서 Eclipse 실행 확인이 필요하다.
- 사용자 확인 필요 사항: 식사 유형 미선택 상태에서 저장 시 "식사 유형을 선택해주세요." 안내가 표시되고 식단이 저장되지 않는지 확인한다.

### 2026-07-12 / 1단계 식단 저장 후 화면 전환 보정

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: 저장 성공 후 `HomeMeal`로 이동하는 정상 경로 외에 컴포넌트 타입으로 전환 실패를 추측하고 등록되지 않은 `showPanel("Home")`을 호출하는 대체·예외 경로가 있었다.
- 수정 파일: `src/panel/FoodListPanel.java`
- 변경 내용: 식사별 칼로리 정보를 갱신한 뒤 등록된 `HomeMeal` 화면으로 복귀하는 단일 EDT 전환 경로로 정리했다.
- 검증 명령: `git diff --check`, 전체 Java 소스의 `showPanel("Home")` 정적 검색, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 등록되지 않은 `showPanel("Home")` 호출 없음, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `ad0b301 수정: 식단 저장 후 화면 전환 보정`
- 남은 위험: 저장 성공 안내 후 `HomeMeal` 카드 갱신과 화면 복귀의 실제 표시 타이밍은 체크포인트 1에서 Eclipse 실행 확인이 필요하다.
- 사용자 확인 필요 사항: 정상 저장 후 식사 기록 홈으로 복귀하고 선택한 식사 유형의 칼로리 카드가 갱신되는지 확인한다.

### 2026-07-12 / 1단계 식단 저장 오류 안내 보강

- 상태: 정적 검증 및 기능 커밋 완료, 체크포인트 1 사용자 테스트 대기
- 분석 내용: DAO의 실패 반환값을 예외로 변환해 처리하면서 DB 오류 문구를 사용자 안내에 덧붙였고, 저장 경로의 정상 로그가 사용자 ID·선택 음식·섭취량을 출력했다.
- 수정 파일: `src/panel/FoodListPanel.java`, `src/DB/MealLogDAO.java`
- 변경 내용: 식사 기록 생성 실패와 음식 상세 저장 실패를 구분해 안내하고 즉시 반환하여 입력 목록을 유지했다. 성공 안내와 패널 초기화는 두 저장이 모두 성공한 뒤에만 실행되도록 유지했다. 개인정보성 정상 로그를 제거하고 오류 진단 로그는 유지했다.
- 검증 명령: `git diff --check`, 식단 저장 경로 개인정보성 콘솔 출력 정적 검색, 실패 반환·성공 안내·초기화 호출 순서 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 개인정보성 정상 출력 없음, 실패 시 성공 안내 및 초기화 미실행 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `0b302c7 수정: 식단 저장 오류 안내 보강`
- 남은 위험: `meal` 생성과 `meal_log` 저장이 서로 다른 DAO 트랜잭션이므로 상세 저장만 실패하면 빈 식사 헤더가 남을 수 있다. 트랜잭션 구조 변경과 실제 DB 실패 검증은 이번 최소 수정 및 자동 검증 범위에 포함하지 않았다.
- 사용자 확인 필요 사항: 정상 저장에서는 성공 안내 후 식사 기록 홈으로 복귀하는지 확인한다. 실제 오류가 발생하면 성공 안내나 빈 화면 없이 현재 입력 흐름과 오류 안내가 유지되는지 확인하되, 오류 유도를 위해 DB 설정이나 데이터를 변경하지 않는다.

### 2026-07-12 / 체크포인트 1 식단 카드 추가 진입 복구

- 상태: 정적 검증 및 기능 커밋 완료, 사용자 Eclipse 재테스트 대기
- 사용자 테스트 결과: 식단 기록 탭과 아침·점심·저녁·간식 카드는 표시되지만 추가 버튼이 보이지 않고 카드 클릭으로 음식 검색 화면에 진입할 수 없었다. 네비게이션은 통과했으며 macOS TSM 및 IMKCFRunLoop 메시지는 프로젝트 오류에서 제외했다.
- 분석 내용: 기존 의도는 카드 전체 클릭이 아니라 카드 우측 상단의 별도 `JButton`이었다. 네 버튼은 카드에 추가되고 유효한 bounds와 `ActionListener`를 가지지만, 버튼 표시를 Windows 절대 경로의 십자가 이미지에만 의존하고 텍스트와 테두리가 없어 macOS에서 빈 클릭 영역으로 남았다. 카드 자체에는 `MouseListener`가 없다.
- 수정 파일: `src/panel/HomeMealPanel.java`
- 변경 내용: 절대 경로 십자가 이미지 버튼을 흰색 테두리와 `추가` 텍스트, 손 모양 커서를 가진 공통 버튼으로 교체했다. 기존 네 식사 유형별 `setMealType()`과 `showPanel("foodList")` 리스너는 유지했고 물 섭취 카드는 변경하지 않았다.
- 검증 명령: `git diff --check`, 수정 diff 직접 확인, 네 버튼의 생성·add·ActionListener·식사 유형·`foodList` 전환 정적 검색, `MainUserPanel` 인스턴스 및 패널 이름 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 네 식사 유형 모두 동일 방식의 이벤트 연결 확인, `foodList` 등록 이름 일치, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다. 관련 없는 UI와 DAO 변경 없음.
- 커밋: `73e27a6 수정: 식단 카드 추가 진입 동작 복구`
- 남은 위험: macOS Swing에서 버튼의 실제 가시성·클릭과 검색 화면 전환은 Eclipse 실행 재확인이 필요하다.
- 사용자 확인 필요 사항: 아침·점심·저녁·간식 각각에서 `추가` 버튼 표시와 손 모양 커서, 클릭 후 해당 식사 유형이 유지된 음식 검색 화면 진입을 확인한 뒤 상세·담기·저장·`HomeMeal` 복귀까지 재검증한다.

### 2026-07-12 / 체크포인트 1 최종 사용자 검증

- 상태: 사용자 Eclipse 테스트 통과
- 확인 완료: 일반 사용자 로그인, 최초 홈과 네비게이션, 네 식사 유형 추가 버튼, 음식 검색·상세·중량 조절·담기·저장, 성공 안내, `HomeMeal` 복귀와 식사 카드 열량 갱신이 모두 정상 동작했다.
- 콘솔 결과: 네비게이션 초기화 경고와 새로운 Java 예외가 관찰되지 않았다. macOS의 `TSM AdjustCapsLockLEDForKeyTransitionHandling` 및 `IMKCFRunLoopWakeUpReliable` 메시지는 프로젝트 오류에서 제외한다.
- 후속 작업: 식단 기능은 정상으로 확정한다. 식단 화면의 디자인·편의성·사용자 경험 개선은 별도 후속 UI/UX 작업으로 기록하며 현재 안정화 기능과 2단계 캘린더 작업에서는 구현하지 않는다.
- 다음 단계: 2단계 월간 캘린더 구현

### 2026-07-12 / 2단계 월간 캘린더 날짜 탐색

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: 기존 `CalendarPanel`은 오늘 운동 기록 목록만 표시하며 월간 날짜 그리드, 월 이동과 날짜 선택 상태가 없었다.
- 수정 파일: `src/panel/CalendarPanel.java`
- 변경 내용: 현재 월 기준 6주 날짜 그리드, 이전 달·다음 달 이동, 연도 경계 이동, 오늘 날짜와 선택 날짜 구분, 날짜 선택과 월 이동 시 선택 상태 갱신을 구현했다. `MainFrame` 440x956 및 캘린더 표시 영역 440x736을 유지했다.
- 검증 명령: `git diff --check`, 월 offset·42개 셀·월 이동·오늘 및 선택 분기 정적 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 일요일 시작 월간 배열과 선택 이벤트 연결 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `c02a97d 기능: 월간 캘린더 날짜 탐색 구현`
- 남은 위험: macOS Swing에서 날짜 셀 크기, 색상과 월 이동 표시 상태는 체크포인트 2 Eclipse 확인이 필요하다.
- 사용자 확인 필요 사항: 현재 월 배열, 오늘·선택 표시, 이전·다음 달 및 12월/1월 연도 경계 이동을 확인한다.

### 2026-07-12 / 2단계 선택 날짜 식단 및 운동 기록

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: `CalendarDAO`가 비어 있고 기존 화면은 오늘 운동만 `ExerciseLogDAO`로 조회해 선택 날짜 식단과 운동을 함께 표시할 계약이 없었다. 정적 스키마에서 `exercise_log.exercise_date`는 DATETIME, `meal.meal_date`는 DATE이며 기존 사용자·기록 키로 조회 가능함을 확인했다.
- 수정 파일: `src/DB/CalendarDAO.java`, `src/panel/CalendarPanel.java`
- 변경 내용: 사용자 ID와 `LocalDate`로 선택 날짜 운동 기록을 조회하고, 식단은 `meal`·`meal_log`·`food`를 기존 키로 묶어 식사 유형·시간·총열량·음식명을 조회한다. 화면에 운동·식단 섹션과 각각의 기록 없음 안내를 추가했다.
- 검증 명령: `git diff --check`, DAO SQL 컬럼과 `sql/schema.sql` 정적 대조, 선택 날짜 파라미터 전달 및 화면 섹션 연결 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: 초기 컴파일에서 `DBConnectionMgr.getConnection()`의 넓은 예외 계약에 맞지 않는 catch 오류 2건을 관련 범위에서 보정했다. 최종 `git diff --check`와 전체 컴파일 통과, 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `06853a2 기능: 선택 날짜 식단 및 운동 기록 표시`
- 남은 위험: 실제 DB 데이터 기준 날짜 필터 결과와 MySQL `GROUP_CONCAT` 음식명 표시 결과는 체크포인트 2 Eclipse 확인이 필요하다. 조회 오류와 실제 기록 없음은 현재 빈 목록으로 동일하게 표시되며 오류 원인은 콘솔 예외로 구분한다.
- 사용자 확인 필요 사항: 기록이 있는 날짜와 없는 날짜를 각각 선택해 운동·식단 내용, 시간·열량·음식명과 기록 없음 안내를 확인한다.

### 2026-07-12 / 2단계 캘린더 고정 창 레이아웃

- 상태: 정적 검증 및 기능 커밋 완료, 체크포인트 2 사용자 테스트 대기
- 분석 내용: 기록 영역은 고정 창 안에 들어오지만 긴 운동명과 여러 음식명이 단일 행 `JLabel`에서 잘릴 수 있고, 기록 갱신 후 스크롤 상단 이동이 목록 패널 좌표에 의존했다.
- 수정 파일: `src/panel/CalendarPanel.java`
- 변경 내용: 기록 영역을 세로 스크롤 전용으로 고정하고 가로 스크롤을 차단했다. 긴 운동명·음식명을 줄바꿈 가능한 읽기 전용 텍스트로 표시하고 카드 높이와 폭을 고정 창 viewport에 맞췄으며 날짜 변경 시 스크롤을 상단으로 초기화한다.
- 검증 명령: `git diff --check`, 캘린더 및 스크롤 bounds·스크롤 정책·텍스트 줄바꿈·카드 크기 정적 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 440x736 캘린더 표시 영역 안에 날짜 그리드와 390x275 기록 스크롤 배치 확인, 가로 스크롤 비활성화와 세로 스크롤 연결 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `de5ef4f 수정: 캘린더 고정 창 레이아웃 보정`
- 남은 위험: macOS Swing 폰트 렌더링에 따른 줄바꿈, 카드 간격, 스크롤바와 하단 네비게이션의 실제 겹침 여부는 Eclipse 확인이 필요하다.
- 사용자 확인 필요 사항: 기록이 많은 날짜와 긴 음식명이 있는 날짜에서 세로 스크롤, 텍스트 잘림, 가로 스크롤 미표시와 하단 화면 겹침 여부를 확인한다.

### 2026-07-12 / 체크포인트 2 최종 사용자 검증

- 상태: 사용자 Eclipse 테스트 통과
- 확인 완료: 월간 날짜 배열, 오늘·선택 날짜 표시, 이전·다음 달과 여러 달 연속 이동, 12월/1월 연도 경계, 월 이동 후 1일 자동 선택, 기록 없음 안내, 선택 날짜 식단·운동 기록, 세로 스크롤과 고정 창 레이아웃이 모두 정상 동작했다. 가로 스크롤, 하단 네비게이션 겹침, 심한 잘림이나 클릭 불가 요소는 없었다.
- 실제 기록 확인: 2026년 7월 12일 선택 상태에서 운동 기록의 운동명·49 kcal·저장 시간과 식단 기록의 아침 유형·시간·음식명이 실제 저장 내용과 일치했으며 두 기록 영역이 구분되어 표시됐다.
- 콘솔 결과: 새로운 Java 예외가 관찰되지 않았다. macOS의 `TSM AdjustCapsLockLEDForKeyTransitionHandling` 및 `IMKCFRunLoopWakeUpReliable` 메시지는 프로젝트 오류에서 제외한다.
- 다음 단계: 3단계 로그인 비밀번호 UX

### 2026-07-12 / 3단계 로그인 비밀번호 표시 전환

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: 로그인 비밀번호 입력은 `RoundedComponent` 내부의 기존 `JPasswordField`를 사용하며 표시 전환 UI가 없었다. 인증은 `attemptLogin()`에서 기존 `LoginDAO.getUserInfo()`로 이어진다.
- 수정 파일: `src/panel/LoginPanel.java`
- 변경 내용: 기존 마스킹 문자를 보존하는 `보기/숨김` 버튼을 비밀번호 필드 우측에 추가했다. `echoChar`만 전환하고 입력값·caret 위치·포커스를 복원하며 Enter 로그인과 인증·세션 흐름은 변경하지 않았다.
- 검증 명령: `git diff --check`, `JPasswordField`·마스킹 문자·caret·포커스 전환 및 기존 인증 호출 정적 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 보기와 다시 숨김 전환 및 인증 호출 유지 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `a619204 기능: 로그인 비밀번호 표시 전환 추가`
- 남은 위험: macOS Swing에서 버튼 겹침, 실제 caret·포커스 유지와 로그인 동작은 체크포인트 3 Eclipse 확인이 필요하다.
- 사용자 확인 필요 사항: 비밀번호 입력 후 보기·숨김을 반복하고 입력값과 cursor가 유지되는 상태에서 Enter 및 로그인 버튼으로 기존 로그인을 확인한다.

### 2026-07-12 / 3단계 로그인 비밀번호 규칙 안내

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: 로그인 화면에는 회원가입 검증과 동일한 비밀번호 문자 규칙 안내가 없었고, 비밀번호 필드와 Remember me 영역 사이에 안내 한 줄을 배치할 공간이 있었다.
- 수정 파일: `src/panel/LoginPanel.java`
- 변경 내용: 기존 `ValidationUtils.isCreateUserPw()` 계약에 맞춰 "6~20자, 영문과 특수문자를 함께 사용"하도록 안내하는 고정 문구를 추가했다. 로그인 검증과 인증 처리는 변경하지 않았다.
- 검증 명령: `git diff --check`, 안내 문구와 기존 비밀번호 정규식 정적 대조, 고정 창 내부 주변 컴포넌트 bounds 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: `git diff --check` 통과, 규칙 계약 일치와 기존 컴포넌트 비중첩 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `afdd0df 개선: 로그인 비밀번호 입력 규칙 안내`
- 남은 위험: macOS 폰트에서 안내 문구 가독성과 실제 잘림 여부는 체크포인트 3 Eclipse 확인이 필요하다.
- 사용자 확인 필요 사항: 안내 문구가 비밀번호 필드 아래에서 잘리지 않고 Remember me·계정 찾기·로그인 버튼과 겹치지 않는지 확인한다.

### 2026-07-12 / 4단계 휴대폰 기반 아이디 찾기

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: 기존 화면의 인증 전송과 아이디 찾기 버튼은 콘솔 출력만 수행했다. 스키마에 휴대폰 UNIQUE 제약은 없지만 회원가입 흐름이 전화번호 중복을 차단해 애플리케이션 정책은 중복 금지로 확인됐다. 외부 문자 발송 계약은 없어 가짜 인증번호를 만들지 않았다.
- 수정 파일: `src/DB/UserDAO.java`, `src/panel/FindIdPwPhone.java`
- 변경 내용: 이름·휴대폰 형식 검증 후 정확히 한 사용자와 일치할 때만 사용자 확인 상태를 저장하고, 입력이 유지된 경우 아이디만 화면에 표시한 뒤 로그인 화면으로 복귀한다. 여러 계정이 일치하면 임의 계정을 선택하지 않는다.
- 검증 명령: `git diff --check`, 휴대폰 중복 가입 차단 정책과 조회 SQL 정적 확인, 사용자 확인 상태·입력 변경 차단·로그인 복귀 및 개인정보 콘솔 미출력 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: 혼합 줄 끝을 기존 CRLF로 통일한 뒤 `git diff --check` 통과, 아이디 조회 흐름과 정확히 한 계정 조건 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `238dd74 기능: 휴대폰 기반 아이디 찾기 구현`
- 남은 위험: 실제 로컬 DB에 과거 중복 휴대폰 데이터가 있으면 아이디 찾기는 일치 실패로 안내한다. 실제 조회와 고정 창 UI는 체크포인트 3 Eclipse 확인이 필요하다.
- 사용자 확인 필요 사항: 가입된 이름·번호, 미가입 번호, 잘못된 형식과 사용자 확인 후 입력 변경을 각각 시험하고 아이디 표시 후 로그인 복귀를 확인한다.

### 2026-07-12 / 4단계 본인 확인 후 비밀번호 재설정

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: 기존 비밀번호 찾기 영역도 인증 전송과 찾기 버튼의 콘솔 출력만 있었고 기존 비밀번호를 노출하지 않는 갱신 DAO가 없었다.
- 수정 파일: `src/DB/UserDAO.java`, `src/panel/FindIdPwPhone.java`
- 변경 내용: 이름·아이디·휴대폰 일치로 사용자 확인 후 새 비밀번호와 확인값을 입력받아 일치 여부와 기존 6~20자 영문·특수문자 규칙을 검사한다. 기존 비밀번호 SELECT 없이 해당 사용자 한 행의 `user_pwd`만 갱신하며, 완료·취소·뒤로가기 시 확인 상태를 폐기하고 로그인으로 복귀한다.
- 검증 명령: `git diff --check`, 사용자 확인 SELECT와 비밀번호 UPDATE 정적 확인, 기존 비밀번호 미조회·미표시, 규칙·확인값·입력 변경·복귀 분기 및 콘솔 미출력 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: CRLF 통일 후 `git diff --check` 통과, 비밀번호 재설정 전 확인 조건과 UPDATE 대상 한 행 확인, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `aba6d9d 기능: 본인 확인 후 비밀번호 재설정 구현`
- 남은 위험: 실제 DB 갱신 성공과 새 비밀번호 로그인, JOptionPane 크기·포커스·취소 복귀는 체크포인트 3 Eclipse 확인이 필요하다. 저장 방식은 기존 평문 계약을 유지하며 이번 범위에서 변경하지 않았다.
- 사용자 확인 필요 사항: 불일치 사용자, 확인 전 설정 시도, 비밀번호 불일치·규칙 위반·정상 설정·취소를 확인하고 기존 비밀번호가 화면과 콘솔에 나오지 않으며 새 비밀번호로 로그인되는지 확인한다.

### 2026-07-12 / 5단계 사용자 공지사항 목록 및 상세

- 상태: 정적 검증 및 기능 커밋 완료
- 분석 내용: 사용자 공지 목록은 스크롤 없이 절대 좌표 행을 공지 패널에 직접 누적해 공지가 많으면 고정 창 밖으로 잘렸다. 상세 본문에는 기본 스크롤이 있었으나 표시 높이와 가로 차단·스크롤 속도 보강이 필요했다.
- 수정 파일: `src/panel/NoticePanel.java`, `src/panel/NoticeDetailPanel.java`
- 변경 내용: 사용자 목록을 번호·제목·작성자·날짜가 구분된 카드형 BoxLayout과 세로 스크롤로 변경하고 카드 전체에서 기존 상세 진입 흐름을 실행한다. 상세 본문 영역을 늘리고 세로 스크롤 속도, 가로 스크롤 차단, 테두리를 보강했으며 정상 화면 전환 디버그 로그를 제거했다.
- 검증 명령: `git diff --check`, 카드 크기·간격·스크롤 정책·전체 클릭 리스너와 기존 DAO·상세 전환 호출 정적 확인, 상세 줄바꿈·고정 bounds 확인, JDK 21 `javac` 전체 `src` 정적 컴파일
- 검증 결과: CRLF 통일 후 `git diff --check` 통과, 기존 조회와 상세 패널 갱신·전환 유지, 컴파일 오류 없음. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건은 유지된다.
- 커밋: `6958b8f 개선: 사용자 공지사항 목록 및 상세 스크롤 보강`
- 남은 위험: macOS 폰트에서 긴 제목 잘림, 카드 클릭 범위, 많은 공지와 긴 본문 스크롤 및 첨부파일 영역 배치는 체크포인트 3 Eclipse 확인이 필요하다.
- 사용자 확인 필요 사항: 공지가 많은 목록의 세로 스크롤·카드 정보·전체 카드 클릭과 긴 본문 스크롤·상세 닫기 복귀를 확인한다.
