# 알고먹자 최종 고도화 작업일지

## 현재 상태

- 작업 브랜치: `codex-final-polish`
- 기준 브랜치: `codex-audit`
- 기준 커밋: `4089009`
- 현재 단계: 체크포인트 1 식단 UI/UX 정적 구현 완료
- 마지막 기능 커밋: `4d2da11 수정: 식단 UI 고정 영역 경계 보정`
- 다음 작업: Eclipse에서 식단 홈·검색·상세·담기·저장 전체 흐름 재검증
- 사용자 Eclipse 검증 대기: 예

## 작업 기록

### 2026-07-12 / 체크포인트 1 사전 분석

- 시작 상태: `codex-final-polish` 브랜치, 변경 파일 없음, `codex-audit..HEAD` 차이 없음
- 기존 사용자 검증: 네 식사 유형 선택, 음식 검색·상세·중량 조절·담기·저장, 성공 안내, `HomeMeal` 복귀와 열량 갱신이 안정화 체크포인트 1에서 통과했다.
- 연결 파일: `src/panel/HomeMealPanel.java`, `src/panel/FoodListPanel.java`, `src/panel/FoodInfoPanel.java`, `src/main/MainUserPanel.java`, `src/ui_n_utils/RoundedComponent.java`, `src/ui_n_utils/TabUtil.java`, `src/ui_n_utils/NavUtil.java`
- 이벤트 흐름: 식사 유형 설정 → `foodList` → 음식 상세 `foodInfo` → 담은 목록 → DAO 저장 → `HomeMeal` 갱신·복귀
- 홈 화면 문제: 네 카드의 구성 코드가 반복되고 Windows 절대 이미지 경로 때문에 macOS에서 이미지가 비어 있으며, 긴 사용자 이름과 안내 문구가 겹칠 수 있다. 열량 라벨 폭과 `추가` 버튼 표현도 작고 분산되어 있다.
- 목록 화면 문제: 현재 식사 유형 표시가 없고 검색 버튼이 절대 이미지 경로를 사용한다. 두 목록의 세로 스크롤이 비활성화돼 있으며 빈 목록은 안내 대신 빈 패널로 채워진다. `+`·`-` 버튼과 작은 텍스트는 역할과 가독성이 약하다.
- 상세 화면 문제: 음식명 라벨이 내부 카드 폭을 넘어가며 뒤로가기가 `<`로만 표시된다. 중량 필드는 직접 입력처럼 보이지만 실제 흐름은 10g 단위 버튼 조절이어서 조작 방식이 불명확하다.
- 수정 원칙: DAO·저장 순서·패널 이름·성공 및 실패 분기를 변경하지 않고 UI 생성, bounds, 문구, 스크롤 정책과 시각적 피드백만 최소 수정한다.
- DB·보안 확인: DB 접속·SQL 실행·스키마 변경 없이 정적 분석만 수행했으며 `src/db.properties`를 읽지 않았다.

### 2026-07-12 / 식단 홈 카드 UI 개선

- 상태: 기능 커밋 및 정적 검증 완료
- 수정 파일: `src/panel/HomeMealPanel.java`
- 변경 내용: 사용자 이름과 안내를 두 행으로 분리하고 네 식사 카드를 동일한 188x170 구조로 통일했다. 각 카드에서 식사 유형, `오늘 기록`, 열량과 156x34 `음식 추가` 버튼을 순서대로 구분했다.
- 플랫폼 보정: 네 식사 이미지와 물 섭취 이미지의 Windows 절대 경로 의존을 제거하고 텍스트 정보로 대체했다. 추가 버튼은 흰 배경·녹색 글자·테두리와 손 모양 커서를 명시해 보이는 영역과 클릭 영역을 일치시켰다.
- 레이아웃: 좌우 카드 끝 x=418, 물 카드 끝 x=418, 최하단 y=622로 440px 폭과 `MainUserPanel`의 686px 콘텐츠 영역 안에 배치했다.
- 유지 기능: 아침·점심·저녁·간식별 `setMealType()`과 `showPanel("foodList")`, 사용자 확인, 열량 조회·갱신과 물 섭취 카드 표시는 유지했다.
- 검증 결과: `git diff --check` 통과, JDK 21 전체 `src` 컴파일 통과. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건만 남았다. `HomeMealPanel`에 절대 이미지 경로가 남지 않았고 DAO 파일은 변경하지 않았다.
- 커밋: `147a948 개선: 식단 홈 카드 가독성과 조작 영역 정리`
- 사용자 확인 필요: macOS Eclipse에서 네 카드와 물 카드의 간격·텍스트·버튼 표시, 네 식사 유형 전달, 하단 네비게이션 비중첩을 확인한다.

### 2026-07-12 / 음식 검색·선택·담기 화면 UI 개선

- 상태: 기능 커밋 및 정적 검증 완료, 체크포인트 1 사용자 테스트 대기
- 수정 파일: `src/panel/FoodListPanel.java`, `src/panel/FoodInfoPanel.java`
- 음식 목록 변경: 선택한 식사 유형을 상단에 표시하고 검색 아이콘의 Windows 절대 경로를 90x42 `검색` 텍스트 버튼으로 교체했다. 검색·담은 음식 탭 문구, 담은 개수, 130x42 `식단 화면`과 180x42 `식단 저장` 버튼으로 조작 우선순위를 구분했다.
- 목록 가독성: 검색·담은 목록에 세로 `AS_NEEDED`, 가로 `NEVER` 정책을 적용했다. 음식 행을 390x96으로 맞추고 긴 음식명은 두 줄 줄바꿈·전체 이름 툴팁으로 처리했으며 열량·중량과 `담기`·`빼기` 버튼을 분리했다.
- 빈 상태: 검색 결과 없음과 담은 음식 없음에 각각 중립 안내 카드를 표시한다. DAO 예외 stack trace는 유지해 조회 오류가 성공으로 기록되지 않도록 기존 진단 흐름을 보존했다.
- 음식 상세 변경: 현재 식사 유형과 긴 음식명을 400px 카드 안에 표시하고 `<`를 100x40 `목록으로`로 교체했다. 중량은 `-10g`, 읽기 전용 현재값, `+10g`로 구분하고 240x48 `담은 목록에 반영` 버튼으로 역할을 명확히 했다.
- 레이아웃: 목록의 스크롤 하단 y=604, 하단 버튼 y=620~662로 686px 콘텐츠 영역 안에 배치했다. 상세 메인 카드는 x=20~420, y=20~670으로 440x736 표시 영역 안에 배치했다.
- 유지 기능: `setMealType()`, `showPanel("foodList")`·`showPanel("foodInfo")`·`showPanel("HomeMeal")`, 음식 상세 진입, 중량 계산, 담기·수정·빼기와 기존 저장 검증·DAO 호출 순서·성공 후 초기화 순서를 유지했다.
- 검증 결과: `git diff --check` 통과, JDK 21 전체 `src` 컴파일 통과. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건만 남았다. 식단 세 패널에 절대 이미지 경로와 개인정보성 식단 로그가 없고 DAO 파일 변경도 없다.
- 커밋: `6e94d55 개선: 음식 검색 및 선택 화면 사용성 보강`
- 사용자 확인 필요: 네 식사 유형 표시, 검색·긴 음식명·빈 결과, 목록 세로 스크롤·가로 스크롤 미표시, 상세 중량 조절, 담기·빼기·목록 복귀, 저장 성공·실패와 홈 열량 갱신을 Eclipse에서 확인한다.

### 2026-07-12 / 식단 UI 고정 영역 경계 보정

- 상태: 기능 커밋 및 정적 검증 완료
- 수정 파일: `src/panel/HomeMealPanel.java`, `src/panel/FoodListPanel.java`
- 변경 내용: `HomeMealPanel` 자체 높이를 `MainUserPanel` 콘텐츠 영역과 같은 686px로 맞췄다. 검색 결과 없음과 담은 음식 없음 안내를 340px 폭 HTML 문단으로 표시해 긴 안내가 잘리지 않도록 보강했다.
- 검증 결과: `git diff --check`와 JDK 21 전체 `src` 컴파일 통과. 기존 제거 예정 API 경고 1건만 남았으며 이벤트·DAO·화면 전환 변경은 없다.
- 커밋: `4d2da11 수정: 식단 UI 고정 영역 경계 보정`
