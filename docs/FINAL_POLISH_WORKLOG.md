# 알고먹자 최종 고도화 작업일지

## 현재 상태

- 작업 브랜치: `codex-final-polish`
- 기준 브랜치: `codex-audit`
- 기준 커밋: `4089009`
- 현재 단계: 체크포인트 4 식단 저장 트랜잭션 사용자 검증 완료
- 마지막 기능 커밋: `78b807f 개선: 식단 저장을 단일 트랜잭션으로 통합`
- 다음 작업: 체크포인트 5 비밀번호 단방향 해시 전환 사전 분석
- 사용자 Eclipse 검증 대기: 아니요

## 작업 기록

### 2026-07-12 / 체크포인트 1 사전 분석

- 시작 상태: `codex-final-polish` 브랜치, 변경 파일 없음, `codex-audit..HEAD` 차이 없음
- 기존 사용자 검증: 네 식사 유형 선택, 음식 검색·상세·중량 조절·담기·저장, 성공 안내, `HomeMeal` 복귀와 열량 갱신이 안정화 체크포인트 1에서 통과했다.
- 연결 파일: `src/panel/HomeMealPanel.java`, `src/panel/FoodListPanel.java`, `src/panel/FoodInfoPanel.java`, `src/main/MainUserPanel.java`, `src/ui_n_utils/RoundedComponent.java`, `src/ui_n_utils/TabUtil.java`, `src/ui_n_utils/NavUtil.java`
- 이벤트 흐름: 식사 유형 설정 → `foodList` → 음식 상세 `foodInfo` → 담은 목록 → DAO 저장 → `HomeMeal` 갱신·복귀
- 홈 화면 문제: 네 카드의 구성 코드가 반복되고 Windows 절대 이미지 경로 때문에 macOS에서 이미지가 비어 있으며, 긴 사용자 이름과 안내 문구가 겹칠 수 있다. 열량 라벨 폭과 `추가` 버튼 표현도 작고 분산되어 있다.
- 목록 화면 문제: 현재 식사 유형 표시가 없고 검색 버튼이 절대 이미지 경로를 사용한다. 두 목록의 세로 스크롤이 비활성화돼 있으며 빈 목록은 안내 대신 빈 패널로 채워진다. `+`·`-` 버튼과 작은 텍스트는 역할과 가독성이 약하다.
- 상세 화면 문제: 음식명 라벨이 내부 카드 폭을 넘어가며 뒤로가기가 `<`로만 표시된다. 중량 필드는 직접 입력처럼 보이지만 실제 흐름은 10g 단위 버튼 조절이어서 조작 방식이 불명확하다.
- 수정 원칙: DAO·저장 순서·패널 이름·성공 및 실패 분기를 변경하지 않고 UI 생성, bounds, 문구, 스크롤 정책과 시각적 피드백만 최소 수정한다.
- DB·보안 확인: DB 접속·SQL 실행·스키마 변경 없이 정적 분석만 수행했으며 `src/db.properties`를 읽지 않았다.

### 2026-07-12 / 식단 홈 카드 UI 개선

- 상태: 기능 커밋 및 정적 검증 완료
- 수정 파일: `src/panel/HomeMealPanel.java`
- 변경 내용: 사용자 이름과 안내를 두 행으로 분리하고 네 식사 카드를 동일한 188x170 구조로 통일했다. 각 카드에서 식사 유형, `오늘 기록`, 열량과 156x34 `음식 추가` 버튼을 순서대로 구분했다.
- 플랫폼 보정: 네 식사 이미지와 물 섭취 이미지의 Windows 절대 경로 의존을 제거하고 텍스트 정보로 대체했다. 추가 버튼은 흰 배경·녹색 글자·테두리와 손 모양 커서를 명시해 보이는 영역과 클릭 영역을 일치시켰다.
- 레이아웃: 좌우 카드 끝 x=418, 물 카드 끝 x=418, 최하단 y=622로 440px 폭과 `MainUserPanel`의 686px 콘텐츠 영역 안에 배치했다.
- 유지 기능: 아침·점심·저녁·간식별 `setMealType()`과 `showPanel("foodList")`, 사용자 확인, 열량 조회·갱신과 물 섭취 카드 표시는 유지했다.
- 검증 결과: `git diff --check` 통과, JDK 21 전체 `src` 컴파일 통과. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건만 남았다. `HomeMealPanel`에 절대 이미지 경로가 남지 않았고 DAO 파일은 변경하지 않았다.
- 커밋: `147a948 개선: 식단 홈 카드 가독성과 조작 영역 정리`
- 사용자 확인 필요: macOS Eclipse에서 네 카드와 물 카드의 간격·텍스트·버튼 표시, 네 식사 유형 전달, 하단 네비게이션 비중첩을 확인한다.

### 2026-07-12 / 음식 검색·선택·담기 화면 UI 개선

- 상태: 기능 커밋 및 정적 검증 완료, 체크포인트 1 사용자 테스트 대기
- 수정 파일: `src/panel/FoodListPanel.java`, `src/panel/FoodInfoPanel.java`
- 음식 목록 변경: 선택한 식사 유형을 상단에 표시하고 검색 아이콘의 Windows 절대 경로를 90x42 `검색` 텍스트 버튼으로 교체했다. 검색·담은 음식 탭 문구, 담은 개수, 130x42 `식단 화면`과 180x42 `식단 저장` 버튼으로 조작 우선순위를 구분했다.
- 목록 가독성: 검색·담은 목록에 세로 `AS_NEEDED`, 가로 `NEVER` 정책을 적용했다. 음식 행을 390x96으로 맞추고 긴 음식명은 두 줄 줄바꿈·전체 이름 툴팁으로 처리했으며 열량·중량과 `담기`·`빼기` 버튼을 분리했다.
- 빈 상태: 검색 결과 없음과 담은 음식 없음에 각각 중립 안내 카드를 표시한다. DAO 예외 stack trace는 유지해 조회 오류가 성공으로 기록되지 않도록 기존 진단 흐름을 보존했다.
- 음식 상세 변경: 현재 식사 유형과 긴 음식명을 400px 카드 안에 표시하고 `<`를 100x40 `목록으로`로 교체했다. 중량은 `-10g`, 읽기 전용 현재값, `+10g`로 구분하고 240x48 `담은 목록에 반영` 버튼으로 역할을 명확히 했다.
- 레이아웃: 목록의 스크롤 하단 y=604, 하단 버튼 y=620~662로 686px 콘텐츠 영역 안에 배치했다. 상세 메인 카드는 x=20~420, y=20~670으로 440x736 표시 영역 안에 배치했다.
- 유지 기능: `setMealType()`, `showPanel("foodList")`·`showPanel("foodInfo")`·`showPanel("HomeMeal")`, 음식 상세 진입, 중량 계산, 담기·수정·빼기와 기존 저장 검증·DAO 호출 순서·성공 후 초기화 순서를 유지했다.
- 검증 결과: `git diff --check` 통과, JDK 21 전체 `src` 컴파일 통과. 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건만 남았다. 식단 세 패널에 절대 이미지 경로와 개인정보성 식단 로그가 없고 DAO 파일 변경도 없다.
- 커밋: `6e94d55 개선: 음식 검색 및 선택 화면 사용성 보강`
- 사용자 확인 필요: 네 식사 유형 표시, 검색·긴 음식명·빈 결과, 목록 세로 스크롤·가로 스크롤 미표시, 상세 중량 조절, 담기·빼기·목록 복귀, 저장 성공·실패와 홈 열량 갱신을 Eclipse에서 확인한다.

### 2026-07-12 / 식단 UI 고정 영역 경계 보정

- 상태: 기능 커밋 및 정적 검증 완료
- 수정 파일: `src/panel/HomeMealPanel.java`, `src/panel/FoodListPanel.java`
- 변경 내용: `HomeMealPanel` 자체 높이를 `MainUserPanel` 콘텐츠 영역과 같은 686px로 맞췄다. 검색 결과 없음과 담은 음식 없음 안내를 340px 폭 HTML 문단으로 표시해 긴 안내가 잘리지 않도록 보강했다.
- 검증 결과: `git diff --check`와 JDK 21 전체 `src` 컴파일 통과. 기존 제거 예정 API 경고 1건만 남았으며 이벤트·DAO·화면 전환 변경은 없다.
- 커밋: `4d2da11 수정: 식단 UI 고정 영역 경계 보정`

### 2026-07-12 / 체크포인트 1 부분 통과 및 음식 목록 카드 보정

- 상태: 홈 식단 카드와 음식 상세 화면 사용자 검증 통과, 음식 검색·담은 목록 보정 완료, 사용자 재검증 대기
- 사용자 통과 항목: 홈 이름·안내 비중첩, 네 식사 카드 일관성, 음식 추가 버튼, 물 카드·하단 네비게이션 비중첩, 음식 상세 영양 정보와 버튼 배치, 440x956 고정 창 표시가 정상이다.
- 보정 원인: 목록 콘텐츠가 고정 최대 폭 wrapper와 절대 bounds 기반 `RoundedComponent`를 함께 사용해 `BoxLayout`·viewport 폭 변화에 따라 실제 행 폭과 내부 조작 영역이 일치하지 않았다. 오른쪽에 배치한 버튼이 축소·클리핑 영역에 들어가 담은 목록의 `빼기`가 보이지 않았다.
- 수정 파일: `src/panel/FoodListPanel.java`, `src/panel/FoodInfoPanel.java`
- 카드 폭 보정: 검색·담은 콘텐츠 패널이 `Scrollable.getScrollableTracksViewportWidth()`에서 `true`를 반환하도록 바꿨다. 행은 가로 최대 크기를 viewport에 맡기는 일반 `JPanel`과 `BorderLayout`으로 구성해 좌우 6px 콘텐츠 여백을 제외한 가용 폭을 사용한다.
- 검색 카드: 긴 음식명 두 줄·툴팁, `기준 중량 100g`, `열량 ... kcal`을 구분하고 오른쪽에 90x42 `상세 보기` 버튼을 배치했다. 카드·이름·중량·열량 영역의 상세 진입 리스너는 유지했다.
- 담은 카드: `적용 중량 ...g`, 계산 열량과 오른쪽 90x42 `빼기` 버튼을 표시한다. 버튼은 해당 행이 캡처한 `FoodBean` 객체를 `removeFavoriteItem(food)`에 전달하고 제거 직후 탭 개수를 갱신한다.
- 중량 단위: 상세 화면의 생성·초기화·증감 표시만 `100g` 형식으로 바꿨다. 내부 `weightValue` 정수, 10~500 범위, 계산과 `FoodBean.setWeight(weightValue)`는 변경하지 않았다.
- 유지 범위: 두 목록의 세로 `AS_NEEDED`·가로 `NEVER`, 빈 상태, 하단 버튼 bounds와 기존 음식 상세·담기·저장·복귀 흐름을 유지했다. DAO·SQL·스키마 변경은 없다.
- 검증 결과: `git diff --check` 통과, JDK 21 전체 `src` 컴파일 통과. 기존 `DBConnectionMgr.finalize()` 경고 1건만 남았다. 절대 이미지 경로와 개인정보성 식단 로그 추가 없음.
- 커밋: `258e6a8 개선: 음식 목록 카드 정보와 조작 영역 보강`
- 사용자 확인 필요: 검색·담은 카드가 스크롤 viewport 폭을 사용하는지, `상세 보기`·`빼기`가 잘리지 않고 동작하는지, 탭 개수 즉시 갱신, 다건 세로 스크롤, `100g` 및 ±10g 표시를 확인한다.

### 2026-07-12 / 체크포인트 1 식단 UI/UX 최종 사용자 검증

- 상태: Eclipse 사용자 검증 통과, 체크포인트 1 완료
- 홈 식단: 카드 배치, 네 식사 유형, 음식 추가 버튼과 하단 비중첩이 정상이다.
- 검색·상세: 검색 결과 카드 전체 폭, 음식명·기준 중량·열량, 상세 보기, 최초 `100g`, 중량 조절과 열량 갱신이 정상이다.
- 담은 음식: 카드 전체 폭, 적용 중량·열량·빼기 버튼과 제거 후 개수 즉시 갱신이 정상이다.
- 저장 흐름: 음식 두 개 저장, 성공 안내, `HomeMeal` 복귀가 정상이다. 저녁 카드에는 저장한 두 음식의 89 kcal와 78 kcal 합계인 167 kcal가 표시됐다.
- 화면·콘솔: 겹침과 가로 스크롤이 없고 새로운 Java 예외가 관찰되지 않았다.
- 결론: 식단 UI/UX 체크포인트 1을 완료 상태로 확정하고 기존 기능·DAO 계약을 유지한다.
- 다음 단계: 체크포인트 2 공지사항 실행 검증 준비를 위한 관리자 접근 경로 정적 분석

### 2026-07-12 / 체크포인트 2 관리자 접근 및 공지 검증 경로 분석

- 상태: 코드 변경 없는 정적 분석 완료, 기존 정상 관리자 로그인 확인 대기
- 관리자 인증 구조: 일반 회원의 `user` 테이블·권한 컬럼을 사용하는 방식이 아니라 별도 `admin` 테이블의 ID·비밀번호 일치 여부를 먼저 조회한다. 조회 성공 시 `UserBean` 역할을 `ADMIN`으로 설정하고 `UserSessionManager` 관리자 세션을 만든 뒤 `mainAdmin`으로 이동한다. 특정 관리자 ID를 하드코딩한 분기는 없다.
- 일반 사용자 분기: 관리자 조회가 일치하지 않을 때만 `user` 테이블 로그인을 조회하고 역할을 `USER`로 설정한다.
- 계정 찾기 적용 범위: `FindIdPwPhone`은 `UserDAO`의 `user` 테이블 전용 이름·휴대폰 조회, 사용자 확인과 비밀번호 갱신만 호출한다. 별도 `admin` 테이블에는 이름·휴대폰 기반 확인·재설정 경로가 없어 관리자 계정에 적용할 수 없다.
- 관리자 생성·권한 부여: 앱에는 관리자 회원가입, 일반 회원의 관리자 승격, 관리자 비밀번호 재설정 UI나 DAO가 없다. 코드에서 관리자 행을 추가·수정하는 정상 기능도 확인되지 않았다.
- 기존 정상 접근 경로: `schema.sql`에 관리자 테이블 생성과 테스트 관리자 seed가 있고 `README_DB_CONFIG.md`에 로컬 테스트 관리자 안내 절이 있음을 파일·문서 구조로만 확인했다. 계정 값과 비밀번호 평문은 읽거나 기록하지 않았다. 사용자가 자신의 로컬 문서에서 해당 정보를 직접 확인해 일반 로그인 화면에 입력하는 경로만 현재 앱의 정상 관리자 접근 방법이다.
- 차단 조건: 로컬 문서의 계정이 현재 DB와 일치하지 않거나 사용자가 별도로 알고 있는 관리자 계정이 없으면 앱 내부 복구는 불가능하다. 이 경우 관리자 행 생성 또는 비밀번호 변경이라는 DB 데이터 변경이 필요하므로 현재 작업에서는 실행하지 않고 중단해야 한다.
- 사용자 공지 준비: 사용자 목록은 세로 `AS_NEEDED`·가로 `NEVER`, 카드와 모든 정보 라벨의 상세 진입 리스너가 연결돼 있다. 상세 본문은 줄바꿈·세로 `AS_NEEDED`·가로 `NEVER`이며 목록 복귀가 `Notice`로 연결된다.
- 사용자 공지 데이터 조건: 다건 스크롤 검증에는 여러 공지가, 긴 본문 검증에는 충분히 긴 본문 공지가 필요하다. 사용자 화면에는 작성 기능이 없으므로 기존 데이터가 없다면 정상 관리자 작성 흐름이 선행돼야 한다.
- 관리자 목록·상세: 목록은 공지 수에 따라 52px 행 높이로 preferred height를 늘리고 세로 `AS_NEEDED`·가로 `NEVER`를 사용한다. 제목 클릭은 실제 `notice_num`으로 상세를 조회한다. 상세 본문은 줄바꿈·세로 스크롤·가로 차단이 설정돼 있다.
- 작성: 관리자 세션의 ID로 `NoticeDAO.addNotice()`를 호출하고 목록을 다시 불러온 뒤 `NoticeAdmin`으로 복귀한다. 첨부 목록이 비어 있으면 업로드 반복을 건너뛰므로 첨부파일 없는 공지 작성이 가능하다.
- 수정: 상세가 보관한 실제 `notice_num`을 편집 화면에 전달하고 `NoticeDAO.updateNotice()` 성공 후 목록을 다시 불러온다.
- 삭제: 행마다 체크박스 다음 컴포넌트에 실제 `notice_num` 라벨이 추가되는 현재 5개 컴포넌트 순서와 `i * 5 + 1` 조회가 일치한다. 선택 ID를 `NoticeDAO.deleteNotice()`에 전달하고 완료 후 목록을 다시 불러온다.
- 정적 위험: 사용자·관리자 목록과 상세 제목은 고정 폭 단일 행 `JLabel`이라 매우 긴 제목은 잘릴 수 있다. 작성은 제목·본문 공백을 UI에서 명시적으로 차단하지 않으며, 새 공지 ID를 insert 생성 키 대신 최신 공지 조회로 구해 동시 작성 시 위험이 있다. 첨부파일 편집은 이번 무첨부 검증 범위에서 제외한다.
- 보안·DB 제한: 관리자 인증 우회, 비밀번호 조회·출력, DB 접속, SQL 실행, seed 실행과 데이터 변경을 하지 않았다. `src/db.properties`에도 접근하지 않았다.
- 사용자 확인 필요: 사용자가 로컬 문서에서 직접 확인한 기존 관리자 계정으로 정상 로그인한 뒤 무첨부 다건·긴 본문 공지를 작성하고 관리자 CRUD와 사용자 목록·상세를 순서대로 검증한다.

### 2026-07-12 / 체크포인트 2 공지 실행 검증 및 사용자 카드 보정

- 상태: 공지 기능 실행 검증 통과, 사용자 목록 카드 UI 기능 커밋 완료
- 관리자 검증 완료: 정상 로그인, 다건·무첨부 작성, 작성 후 목록 갱신, 상세 진입, 긴 본문 줄바꿈·세로 스크롤, 수정 반영, 단일·다중 선택 삭제, 선택 대상만 삭제와 삭제 후 목록 갱신이 정상 동작했다.
- 사용자 검증 완료: 다건 목록 세로 스크롤, 카드 빈 여백 클릭, 상세 진입, 긴 본문, 상세 닫기 후 목록 복귀가 정상 동작했다. 새로운 Java 예외는 관찰되지 않았다.
- 보정 원인: 사용자 목록의 고정 360px 최대 카드와 일반 `BoxLayout`이 viewport 폭을 추적하지 않아 실제 카드가 가용 폭보다 좁게 배치됐고 우측 작성일 영역이 보이지 않았다.
- 수정 파일: `src/panel/NoticePanel.java`
- 폭 보정: 목록 패널이 `Scrollable.getScrollableTracksViewportWidth()`에서 `true`를 반환하고 카드 최대 폭을 viewport에 맡기도록 변경했다. 좌우 12px 카드 내부 여백만 유지하며 가로 `NEVER`·세로 `AS_NEEDED` 정책은 유지했다.
- 정보 배치: 상단 행은 번호와 제목, 하단 행은 작성자와 `yyyy-MM-dd HH:mm` 작성일을 좌우 배치했다. 제목에는 전체 문자열 툴팁을 추가했다.
- 클릭 유지: 카드, 제목 행, 정보 행과 번호·제목·작성자·작성일 라벨 모두 기존 상세 진입 리스너를 공유한다.
- 검증 결과: `git diff --check`와 JDK 21 전체 `src` 컴파일 통과. DAO·SQL·패널 전환·개인정보성 로그 변경 없음. 기존 `DBConnectionMgr.finalize()` 경고 1건만 남았다.
- 커밋: `1814689 개선: 사용자 공지 카드 정보와 폭 보강`
- 사용자 확인 필요: 카드 전체 폭, 번호·제목·작성자·작성일 표시, 전체 제목 툴팁, 카드 전체 클릭과 다건 세로 스크롤을 재검증한다.

### 2026-07-12 / 관리자 공지 제목 및 상세 날짜 표시 보정

- 상태: 기능 커밋 및 정적 검증 완료, 체크포인트 2 UI 재검증 대기
- 수정 파일: `src/panel/NoticeAdminPanel.java`, `src/panel/AdminNoticeDetailPanel.java`
- 관리자 제목: 목록 제목 라벨에 DAO에서 받은 전체 공지 제목을 툴팁으로 연결했다. 기존 한 줄 말줄임, 제목 클릭 상세 진입과 삭제용 컴포넌트 순서는 유지했다.
- 상세 날짜 원인: 작성자 `(10,95,250,20)`와 날짜 `(200,95,180,20)`가 같은 행에서 60px 겹쳤고 날짜 폭이 전체 초 단위 시각을 표시하기에 부족했다.
- 상세 날짜 보정: 작성자를 `(10,95,360,20)`, 날짜를 `(10,118,360,20)`의 별도 행으로 배치하고 날짜를 왼쪽 정렬했다. 구분선은 y=145, 본문 스크롤은 `(10,155,360,325)`로 조정해 기존 하단 파일 영역 시작 y=500을 침범하지 않는다.
- 추가 표시: 관리자 상세 제목에도 전체 제목 툴팁을 연결했다. 날짜 값은 기존 DAO Timestamp와 `yyyy-MM-dd HH:mm:ss` 표시 형식을 유지했다.
- 유지 기능: 관리자 목록·상세·작성·수정·단일·다중 삭제, 실제 Notice ID 연결, 삭제 확인과 목록 갱신, 본문 스크롤과 모든 DAO 호출을 변경하지 않았다.
- 검증 결과: `git diff --check`와 JDK 21 전체 `src` 컴파일 통과. DAO·SQL·스키마 및 개인정보성 로그 변경 없음. 기존 `DBConnectionMgr.finalize()` 경고 1건만 남았다.
- 커밋: `fe9510b 개선: 관리자 공지 제목 및 상세 날짜 표시 보정`
- 사용자 확인 필요: 관리자 목록 긴 제목 툴팁, 상세 작성자·전체 작성 날짜 비중첩, 본문 스크롤과 기존 CRUD 유지 여부를 확인한다.

### 2026-07-12 / 체크포인트 2 공지사항 UI 최종 사용자 검증

- 상태: Eclipse 사용자 검증 통과, 체크포인트 2 완료
- 사용자 목록: 공지 카드가 viewport 가용 폭 대부분을 사용하고 번호·제목과 작성자·작성일이 겹치지 않았다. 작성일은 `yyyy-MM-dd HH:mm` 전체가 표시됐으며 다건 세로 스크롤과 가로 스크롤 차단이 정상이다.
- 사용자 상세: 카드와 하위 정보 클릭 모두 상세로 진입했다. 작성 날짜 전체 표시, 긴 본문 줄바꿈·세로 스크롤, 본문·첨부 영역 비중첩과 닫기 후 공지 목록 복귀가 정상이다.
- 관리자 화면: 목록 정렬, 긴 제목 말줄임과 전체 제목 툴팁, 상세 작성자·전체 작성 날짜와 본문·첨부 영역 비중첩이 정상이다.
- 기존 기능: 관리자 공지 작성·수정·삭제가 기존과 같이 동작했으며 새로운 Java 예외가 관찰되지 않았다.
- 결론: 공지사항 CRUD와 사용자 조회 및 UI 보정을 포함한 체크포인트 2를 완료 상태로 확정한다.
- 다음 단계: 체크포인트 3 로그인 전 세션 없음 정상 로그와 `DBConnectionMgr.finalize()` 제거 예정 경고 정리

### 2026-07-12 / 로그인 초기화 세션 없음 정상 로그 정리

- 상태: 기능 커밋 및 정적 검증 완료
- 발생 원인: `MainFrame`이 로그인 화면을 표시하기 전에 `MainUserPanel`과 사용자 전용 하위 패널을 미리 생성한다. 이때 `HomeDailyPanel`의 30초 갱신 타이머와 최초 갱신, `HomeTargetPanel`의 생성 직후 비동기 조회가 로그인 세션이 없는 정상 상태를 오류처럼 출력했다.
- 일일 현황: 세션 ID가 없으면 DAO 조회 전에 그대로 반환하는 기존 차단 흐름은 유지하고 반복되는 `로그인된 사용자가 없습니다.` 정상 로그만 제거했다.
- 목표 달성: 숨겨진 패널의 로그인 전 초기화에서는 출력 없이 DAO 조회 전에 반환한다. 실제로 화면이 표시됐는데 세션이 없을 때만 `⚠` 경고를 남겨 진단 가능성을 유지했다.
- 유지 로그: 로그인 사용자 전달값이 `null`인 실제 호출 오류, 목표 데이터 조회 실패, UI 초기화 실패, 영양 정보 조회 예외와 스택 트레이스는 변경하지 않았다.
- 호출 범위: `LoginManager`, `UserSessionManager`, 로그인 인증, DAO 공개 메서드와 SQL은 변경하지 않았다. 세션이 없을 때 사용자 전용 DAO가 호출되지 않는 기존 구조도 유지된다.
- 검증 결과: 대상 문구 재검색, 호출 흐름과 diff 직접 검토, `git diff --check`, JDK 21 전체 `src` 컴파일을 수행했다. 컴파일은 통과했고 기존 `DBConnectionMgr.finalize()` 제거 예정 경고 1건만 남았다.
- 커밋: `5a61d94 정리: 로그인 전 세션 없음 정상 로그 제거`

### 2026-07-12 / DB 연결 관리자 finalize 의존 제거

- 상태: 기능 커밋 및 정적 검증 완료, 체크포인트 3 Eclipse 검증 대기
- 기존 역할: `finalize()`가 호출되면 풀에 들어 있는 사용 중·미사용 커넥션을 모두 닫고 목록을 비우도록 구현돼 있었다.
- 호출 분석: `DBConnectionMgr`는 정적 싱글톤 참조로 앱 수명 동안 유지되며 프로젝트 안에서 `finalize()`를 직접 호출하는 코드는 없다. JVM의 비결정적인 객체 소멸 시점에만 기대하는 종료 정리였으므로 정상 DAO 반환 흐름에는 관여하지 않았다.
- 제거 방식: 제거 예정 API인 `finalize()` 재정의만 삭제했다. 별도 `Cleaner`나 `AutoCloseable` 수명 구조는 추가하지 않았다.
- 유지 경로: DAO가 결과 집합·명령문을 닫고 커넥션을 풀에 반환하는 `freeConnection(...)`, 개별 커넥션을 닫는 `removeConnection(...)`, 미사용 커넥션을 해제하는 `releaseFreeConnections()`와 `getConnection()` 계약은 변경하지 않았다.
- 검증 결과: 전체 Java 소스에서 `finalize()` 참조가 없어졌고 `git diff --check`와 JDK 21 전체 `src` 컴파일을 통과했다. 제거 예정 `finalize()` 경고는 사라졌다. `-Xlint:deprecation`에서는 기존 `Class.newInstance()` 사용 경고 1건과 raw `Vector`의 unchecked 안내가 별도 잔여 항목으로 확인됐다.
- 제한 준수: DAO·SQL·스키마를 변경하지 않았고 DB 접속이나 SQL 실행 없이 정적으로 검증했다.
- 커밋: `587750d 정리: DBConnectionMgr finalize 의존 제거`
- 사용자 확인 필요: 앱 시작부터 로그인 화면 대기 중 세션 없음 문구가 반복되지 않는지, 일반 사용자 로그인 후 일일 현황·목표 달성 조회와 기존 주요 DB 기능이 정상인지 확인한다.

### 2026-07-12 / 체크포인트 3 최종 사용자 검증

- 상태: Eclipse 사용자 검증 통과, 체크포인트 3 완료
- 로그인 전 콘솔: 앱 실행 후 대기 중 세션 없음 반복 로그가 출력되지 않았다.
- 사용자 기능: 일일 현황 영양·열량, 식단 기록, 목표 달성 정보가 정상 표시됐다.
- DB 기능: 음식 검색·DB 조회와 공지사항 목록이 정상 동작했고 `DBConnectionMgr.finalize()` 제거 이후 기존 DB 기능에 이상이 없었다.
- 예외·로그: 새로운 Java 예외와 DB 커넥션 오류가 없었다. 콘솔에는 프로젝트 오류에서 제외한 macOS TSM·`IMKCFRunLoop` 시스템 메시지만 관찰됐다.
- 결론: 로그인 전 정상 로그와 `finalize()` 경고 정리를 포함한 체크포인트 3을 완료 상태로 확정한다.
- 다음 단계: 식단 헤더와 상세 저장을 하나의 JDBC 트랜잭션으로 묶는 체크포인트 4

### 2026-07-12 / 식단 저장 단일 트랜잭션 통합

- 상태: 기능 커밋 및 정적 검증 완료, 체크포인트 4 Eclipse 검증 대기
- 기존 위험: `FoodListPanel`이 `MealDAO.insertMeal()`로 헤더를 먼저 저장한 뒤 `MealLogDAO.insertMealLogs()`를 호출했다. 두 메서드가 풀에서 각각 다른 `Connection`을 빌렸고 상세 저장 트랜잭션은 헤더 INSERT를 포함하지 않아, 헤더 성공 후 상세 실패 시 빈 `meal` 행을 되돌릴 수 없었다.
- 설계 선택: 식단 저장 전용 `MealSaveService`를 추가하는 A안을 선택했다. UI에 JDBC 코드를 넣지 않고 트랜잭션 경계를 한 곳에 모으면서 기존 DAO의 독립 호출 메서드를 유지할 수 있어 변경 책임이 가장 명확하다.
- 동일 연결: 서비스가 풀에서 `Connection`을 한 번 빌려 `MealDAO.insertMeal(Connection, ...)`과 `MealLogDAO.insertMealLogs(Connection, ...)` 오버로드에 차례로 전달한다. 두 DAO 오버로드는 전달받은 연결을 닫거나 풀에 반환하지 않는다.
- 트랜잭션 흐름: 기존 `autoCommit` 값을 보관하고 `setAutoCommit(false)` 후 헤더 INSERT·생성 키 확인·모든 상세 INSERT를 수행한다. 모든 작업 성공 후에만 `commit()`하며 헤더 실패, 생성 키 미반환, 상세 1건 실패와 예외는 모두 `rollback()` 경로로 합류한다.
- 오류 복구: 롤백 자체가 실패하면 원래 예외에 suppressed 예외로 연결해 최초 원인을 가리지 않는다. `finally`에서 기존 `autoCommit` 값을 복원한 뒤 커넥션을 한 번 반환하고, 상태 복원이 실패한 연결은 풀에 돌려놓지 않고 제거한다.
- 자원 관리: 헤더의 `PreparedStatement`·생성 키 `ResultSet`과 상세 `PreparedStatement`는 try-with-resources로 닫힌다. 서비스만 트랜잭션 커넥션 수명을 관리한다.
- DAO 호환: 기존 `MealDAO.insertMeal(String, String)`과 `MealLogDAO.insertMealLogs(int, Vector<FoodBean>)` 시그니처는 유지하고 새 오버로드가 실제 INSERT를 공유한다. 기존 SQL 컬럼·날짜/시간 함수·생성 키 방식과 음식 중량·열량 저장 의미는 변경하지 않았다.
- UI 처리: `FoodListPanel`은 서비스의 성공 여부만 확인한다. 실패 시 기존 저장 오류 안내 후 현재 화면과 담은 목록을 유지한다. 성공 안내, `clearPanel()`, `HomeMeal` 열량 갱신과 복귀는 커밋 성공 뒤에만 실행된다. 식사 유형·로그인·빈 목록 사전 차단도 유지된다.
- 검증 결과: 동일 `Connection` 전달, `setAutoCommit(false)`·`commit()`·모든 저장 실패의 `rollback()` 합류, auto-commit 복원, 단일 커넥션 반환, try-with-resources와 UI 성공 분기 순서를 정적으로 확인했다. `git diff --check`와 JDK 21 전체 `src` 컴파일을 통과했다.
- 제한 준수: `sql/schema.sql`의 기존 `meal`·`meal_log` 구조만 정적으로 확인했으며 스키마·SQL 의미를 변경하거나 DB에 접속·실행하지 않았다. 개인정보나 선택 음식 상세를 출력하는 로그를 추가하지 않았다.
- 실행 검증 범위: 정상 다건 저장은 Eclipse에서 검증할 수 있다. 데이터·스키마·설정을 변경하지 않고 상세 INSERT만 안전하게 실패시키는 기존 정상 경로는 없어 롤백 실행 검증은 보류한다. 테스트 전용 강제 예외나 장애 플래그는 추가하지 않았다.
- 커밋: `78b807f 개선: 식단 저장을 단일 트랜잭션으로 통합`

### 2026-07-12 / 체크포인트 4 최종 사용자 검증

- 상태: Eclipse 사용자 검증 통과, 체크포인트 4 완료
- 저장 흐름: 서로 다른 중량의 음식 두 개가 정상 저장됐고 성공 안내는 한 번만 표시됐다.
- 저장 후 화면: 성공 후 `HomeMeal` 복귀와 해당 식사 카드의 합산 열량 표시가 정상이다.
- 기록 조회: 캘린더 선택 날짜에서 식사 유형·음식명·총열량이 정상 표시됐고 앱 재실행 후에도 저장 기록이 유지됐다.
- 예외·연결: 새로운 Java 예외가 없었으며 커넥션 획득·반환과 트랜잭션 관련 오류도 관찰되지 않았다.
- 결론: 정상 다건 저장과 후속 조회를 포함한 체크포인트 4를 완료 상태로 확정한다. 강제 장애 없이 재현할 수 없는 rollback 실패 경로는 앞선 정적 검증 상태를 유지한다.
- 다음 단계: 코드 변경 전 일반 사용자·관리자 비밀번호 해시 전환 사전 분석
